{
   "_id": "1723", /* The LORIS_file_id from the MySQL database */
   "meta": {
       "doctype": "dataset", 
       "file_format": "data.frame", /* see README.md*/
       "variable_type": "Beta values", /* This is used to create the tabs in the genomic browser */
       "variable_format": "float", /* This is how to display those values. Accepted: float, integer, string*/
       "variable_count" : 0, /* This is usualy the number of row in the file that contain data */
       "ad_libitum" : {}, /* Place to put history, provenance information of those data points or any information that should be kept with that dataset */
   },
   "headers": [], /* The header of the variable value columns. The order need to be consistent whit the order of the values puti int the 'values' tag of each variable document */ 
}
