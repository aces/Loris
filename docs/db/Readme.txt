This contains whatever you need when you start a new project.  The alters file contains experimental alters which add lots of foreign key constraints that should exist - however these have not been extensively tested with the full system so use at your own risk.
The default user created has a login SiteMin with password dieseneurodb
